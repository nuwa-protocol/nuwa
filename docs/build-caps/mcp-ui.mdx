---
title: "MCP UI"
description: "Return UI resource from MCP tools"
---


MCP specifications supports [returning resources](https://modelcontextprotocol.io/specification/2025-06-18/server/tools#embedded-resources) along with a tool result. We leveraged this mechanism and defined a resource schema for UI resources. When a tool returns a UI resource, the client will render an iframe that embeds the UI described by this UI resource.

## UI Resource Schema

Here's the UI resource schema:

```ts
interface UIResource {
  uri: string;                  // capui:/<path>
  name: string;                 // display name
  description?: string;
  mimeType: 'text/x-nuwa-capui-path';
  text: string;                 // path
}
```

<Warning>TODO: update the UI resource schema and expalin each field</Warning>

## `createUIResource` 

The `@nuwa-ai/ui-kit` provides a helper function for constructing a UI resource:

```ts
import { createUIResource } from '@nuwa-ai/ui-kit';

// Fine‑grained control over annotations
const resource = createUIResource('/settings', 'Settings', 'Edit preferences', 'artifact');
return { content: [{ type: 'resource', resource }] };
```

<Warning>TODO: update the example above</Warning>

