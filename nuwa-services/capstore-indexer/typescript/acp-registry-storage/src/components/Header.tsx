import React from 'react';

const Header: React.FC = () => {
  return (
    <header>
      <h1>Agent capability yaml upload</h1>
      <p className="subtitle">Call contract registration by uploading YAML files to IPFS</p>
    </header>
  );
};

export default Header;